(URLENCODE string) → urlencoded-string
  If QUERYP is true you get application/x-www-form-urlencoded.

(URLDECODE urlencoded-string) → string
  If LENIENTP is true no error will be thrown on unencoded reserved characters.
  If QUERYP is true you get application/x-www-form-urlencoded.
  EOL decides what line ending to use, can be :CR or :LF or :CRLF

  Will signal URLENCODE-MALFORMED-STRING if urlencoded-string
  is not a valid urlencoded string.
